# Redirect HTTP -> HTTPS
server {
    listen 80;
    server_name testflow-api.duckdns.org;
    return 301 https://$host$request_uri;
}

# HTTPS server
server {
    listen 443 ssl;
    server_name testflow-api.duckdns.org;

    ssl_certificate     /etc/nginx/ssl/fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # Error handling
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }

    # Proxy API requests to the backend
    location /api {
        resolver 127.0.0.11 valid=30s;
        set $upstream_api http://testflow-server:3006;
        proxy_pass $upstream_api;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # Proxy static file uploads (PDFs, etc.) to the backend
    location /uploads {
        resolver 127.0.0.11 valid=30s;
        set $upstream_uploads http://testflow-server:3006;
        proxy_pass $upstream_uploads;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;

        # Cache static files for 1 day
        add_header Cache-Control "public, max-age=86400";
    }

    # Proxy Socket.io requests to the backend
    location /socket.io {
        resolver 127.0.0.11 valid=30s;
        set $upstream_socket http://testflow-server:3006;
        proxy_pass $upstream_socket;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "Upgrade";
        proxy_set_header Host $host;
    }
}
