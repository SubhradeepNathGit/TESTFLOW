services:
  server:
    build:
      context: .
      dockerfile: server/Dockerfile
    container_name: testflow-server
    ports:
      - "${PORT:-3006}:3006"
    volumes:
      - ./server/uploads:/usr/src/app/server/uploads
    env_file:
      - ./server/.env
    restart: always
    networks:
      - testflow-network

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: testflow-client
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /etc/letsencrypt/live/testflow-api.duckdns.org/fullchain.pem:/etc/nginx/ssl/fullchain.pem:ro
      - /etc/letsencrypt/live/testflow-api.duckdns.org/privkey.pem:/etc/nginx/ssl/privkey.pem:ro
    restart: always
    networks:
      - testflow-network
    depends_on:
      - server

networks:
  testflow-network:
    driver: bridge
